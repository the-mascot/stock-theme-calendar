# 계산 방식 정리

화면에 보이는 숫자가 **어디서, 어떤 식으로** 만들어지는지 한 곳에 모은 문서예요.
계산은 두 군데에서 일어나요.

| 단계 | 위치 | 하는 일 |
|---|---|---|
| ① 배치 (매일) | `Build.py` | 종가 → 종목 등락률 → 테마 등락률 → 시장대비 → 일별 1위 테마 → JSON 저장 |
| ② 화면 (열 때마다) | `src/lib/*.ts`, 컴포넌트 | JSON을 읽어 월간 지수·월간 1위 테마·순위·색을 계산 |

```
토스증권 API (일봉 종가)
   │
   ▼  Build.py
종목별 일간 등락률  →  ±29% 절사  →  테마 단순평균(chg)  →  코스피 뺀 값(rel)  →  그날 1위(lead)
   │
   ▼  data/YYYY-MM.json  (GitHub Pages)
   │
   ▼  미니앱 (src/)
월간 코스피·코스닥(복리)  ·  월간 1위 테마(일평균)  ·  저조 테마  ·  색 5단계
```

설정값(절사 기준, 최소 유효 비율 등)은 전부 `themes.yaml`의 `aggregation` 블록에 있어요.

---

## 1. 배치 계산 (`Build.py`)

### 1-1. 종목·지수의 일간 등락률 — `closes_to_chg()`

```
등락률(%) = (오늘 종가 ÷ 전일 종가 − 1) × 100
```

- 일봉 종가만 써요. **수정주가**(`adjusted=true`)라서 액면분할·배당락이 있어도 튀지 않아요.
- 조회한 캔들 중 **가장 오래된 날은 전일 종가가 없어서** 등락률이 안 나와요. 그래서 백필은 필요한 기간보다 15개 더 받아요 (`parse_period`).
- 종가는 API에서 문자열로 오기 때문에 `float`로 바꿔서 계산해요.
- 종목 캔들이 2개 미만이면 그 종목은 통째로 스킵하고 `index.json`의 `skippedTickers`에 남겨요.

### 1-2. 날짜 기준

- 날짜 축은 **코스피의 영업일**이에요. 휴장일은 자동으로 빠져요.
- 오늘 장이 아직 안 끝났으면(`regularMarket.endTime` 이전) **직전 영업일까지만** 집계해요 (`last_complete_date`). 장중 미완성 캔들이 섞이는 걸 막으려는 거예요.

### 1-3. 코스피·코스닥 일별 등락률

1-1 식을 그대로 코스피·코스닥 지수 종가에 적용해요. 소수 둘째 자리까지 반올림해서 `idx.kospi`, `idx.kosdaq`에 저장해요.

**나스닥은 조금 달라요.** 토스 API에 나스닥 지수가 없어서 나스닥100 ETF인 `QQQ`로 대신해요. 한국 날짜 `d`에는 **`d` 바로 이전의 미국 세션** 등락률을 붙여요 (`nasdaq_for`). 한국이 9월 17일 오전에 열릴 때 반영해야 할 건 9월 16일 밤(미국 시간)에 끝난 세션이기 때문이에요. (지금 화면에는 표시하지 않고 데이터에만 저장돼요.)

### 1-4. 테마 일별 등락률 `chg` — `aggregate()`

한 테마의 그날 값은 **바스켓 종목들의 등락률을 단순평균**한 거예요. 시총가중은 안 써요 (대장주 하나에 테마 신호가 묻혀서).

순서는 이래요.

1. 그날 등락률이 있는 종목만 모아요.
2. 각 종목 등락률을 **±29%로 절사**해요 (`outlier_clip`). 상·하한가급 이상치 하나가 평균을 흔드는 걸 막아요.
3. 그 값들의 산술평균을 구해요.

```
chg = (절사된 종목 등락률의 합) ÷ (유효 종목 수)
```

**예시** — 네 종목이 +6.0, +3.0, −1.0, +30.0일 때

```
절사: +6.0, +3.0, −1.0, +29.0     (30 → 29)
chg = (6 + 3 − 1 + 29) ÷ 4 = +9.25%
```

**유효 종목이 너무 적은 날은 그 테마를 아예 비워요.**

```
(그날 등락률이 있는 종목 수) ÷ (수집에 성공한 바스켓 종목 수)  <  0.6   →  그날 그 테마는 없음
```

- 기준은 `min_valid_ratio: 0.6`이에요. 10종목 바스켓이면 최소 6종목이 있어야 해요.
- 비워진 날은 JSON의 `th`에 그 테마가 없고, 캘린더 히트맵에는 **빗금**으로 나와요.
- 살아남은 날의 유효 종목 수는 `n`으로 저장돼요. 상세 시트에서 `n`이 **5 미만이면 "신뢰도 낮음"** 을 붙여요.

### 1-5. 시장대비 `rel`

```
rel = 테마 chg − 코스피 등락률
```

- 같은 날의 코스피 등락률을 빼요 (`relative_to: kospi`).
- 시장이 +2% 오른 날은 모든 테마가 빨갛게 보이니까, 그걸 걷어내고 **테마만의 초과분**을 보려는 값이에요. 순환을 볼 때는 이쪽이 핵심이라 화면 기본값도 `시장대비(rel)`예요.
- 반올림 전 평균에서 빼고 나중에 반올림해서, 화면에서 `chg − 코스피`를 손으로 계산하면 0.01 정도 어긋날 수 있어요.

**실제 데이터 예시** (2026-08-10)

| 항목 | 값 |
|---|---|
| 코스피 | +0.65% |
| 우주항공·방산 chg | +7.17% (8종목 평균) |
| 우주항공·방산 rel | 7.17 − 0.65 = **+6.52%** |

### 1-6. 일별 1위 테마 `lead`

```
lead = 그날 rel이 가장 큰 테마
```

- **항상 `rel` 기준**으로 뽑아서 저장해요. 화면에서 `원본`으로 바꿔도 `lead` 자체는 그대로예요.
- 그날 비워진 테마(`rel`이 없는 테마)는 후보에서 빠져요.
- 동점이면 `themes.yaml`에 먼저 적힌 테마가 이겨요.
- 위 예시 날짜(8/10)는 우주항공·방산 +6.52가 1위, 반도체 +6.37, 2차전지 +5.74 순이었어요.

### 1-7. `top` (테마 내 상위 종목)

절사된 등락률 기준으로 **상위 5개** 종목 `[코드, 등락률]`을 저장해요 (`top_n: 5`). 종목명은 `stocks` 맵에서 찾아요.

### 1-8. 저장 방식

- `--backfill 2y`: 2년치를 월별 파일로 새로 써요.
- `--daily`: 최근 10개 캔들만 받아서 **해당 일자만 덮어써요(upsert)**. 다른 날 데이터는 안 건드려요.

---

## 2. 화면 계산 (`src/`)

### 2-1. 이번 달 vs 지난 달

`App.tsx`가 보고 있는 달에 따라 상단 요약(지수 + 1위 테마) 계산을 바꿔요.

| | 이번 달 (가장 최근 달) | 지난 달 |
|---|---|---|
| 문구 | "○월 ○일 기준" | "○월 한 달 기준" |
| 코스피·코스닥 | 가장 최근 영업일 하루 등락률 | **월간 등락률** (2-2) |
| 1위 테마 | 가장 최근 영업일의 `lead` | **월간 1위 테마** (2-3) |
| 값 라벨 | 종목 평균 등락 | 일평균 등락 |

### 2-2. 지난달 코스피·코스닥 월간 등락률 — `compound()`

**그달 일간 등락률을 복리로 이어 붙여요.** 단순히 더하는 게 아니에요.

```
월간 등락률 = ( ∏ (1 + 일간등락률/100) − 1 ) × 100
```

이 값은 **전월 말 종가 대비 월말 종가 수익률**과 같아요. 그달 첫 영업일의 등락률이 이미 "전월 말 종가 대비"이기 때문이에요.

**실제 데이터 예시** (2026년 8월, 영업일 20일)

| | 복리 (화면 값) | 단순 합계 (참고) |
|---|---|---|
| 코스피 | **+3.39%** | +4.27% |
| 코스닥 | **+15.90%** | +15.45% |

- 등락이 크게 출렁인 달일수록 단순합과 복리가 벌어져요. 8월 코스피는 첫 주에 −5.12%가 있었어서 합계(4.27)보다 실제 수익률(3.39)이 낮아요.
- 그날 지수 값이 없는(`null`) 날은 계산에서 건너뛰어요.

### 2-3. 지난달 1위 테마 — `summarizeMonth()`

**그달 일별 값의 산술평균이 가장 높은 테마**가 1위예요.

```
월평균(테마) = (그달 그 테마의 일별 값 합) ÷ (그 테마 값이 있는 일수)
1위 테마     = 월평균이 가장 큰 테마
```

- "일별 값"은 현재 선택한 기준을 따라요. 기본은 `시장대비(rel)`, `원본`으로 바꾸면 `chg`로 다시 계산해요.
- 빗금(값 없음)인 날은 **0으로 치지 않고 평균에서 빼요**.
- 화면에는 "일평균 등락"이라는 이름으로 그 평균값이 같이 나와요.

**실제 데이터 예시** (2026년 8월)

| 기준 | 1위 | 2위 | 3위 |
|---|---|---|---|
| 시장대비 (rel) | 화장품 +1.38% | 2차전지 +1.32% | SMR·원전 +0.92% |
| 원본 (chg) | 화장품 +1.60% | 2차전지 +1.53% | SMR·원전 +1.13% |

> **"월 1위 테마"와 "일별 1위를 가장 많이 한 테마"는 달라요.**
> 8월에 일별 1위 횟수가 가장 많은 건 2차전지(4회)였고, 월평균 1위는 화장품이었어요.
> 며칠 크게 튄 테마보다 꾸준히 평균을 끌어올린 테마가 월 1위가 되는 구조예요.

### 2-4. "이번 달 저조했던 테마" — `computeWeakest()`

- 테마별 월평균(2-3과 같은 식, 현재 기준 `rel`/`chg`)을 구해서 **낮은 순으로 5개**를 보여줘요.
- 각 줄에 "이번 달 1위 ○회"도 같이 나와요. 그달 `lead`가 그 테마인 날의 수예요. 0회면 "아직 1위 없음" — 아직 순환이 안 온 테마라는 뜻이에요.

### 2-5. 캘린더 칸

- 칸마다 그날 코스피·코스닥 등락률(색으로 방향 표시)과 **그날 1위 테마 이름 + 값**이 들어가요.
- 1위 테마는 JSON의 `lead`(rel 기준)를 그대로 쓰고, 칸에 적히는 값만 현재 기준(`rel`/`chg`)을 따라요.
  그래서 `원본`으로 바꿔도 칸의 1위 테마 이름은 안 바뀌고 숫자만 바뀌어요.

### 2-6. 일별 상세 시트 — `DayDetailSheet`

- 그날 유효한 테마 전체를 현재 기준 값으로 **내림차순 정렬**해서 순위를 매겨요.
- 테마별로 `n종목 평균`, 상위 종목 3개(`top`의 앞 3개), `n < 5`이면 "신뢰도 낮음"을 보여줘요.
- 이 시트는 그날 값을 직접 정렬하기 때문에, `원본`으로 보면 순위 1위가 `lead`와 다를 수 있어요.

### 2-7. 기준 종목 시트 — `collectThemeStocks()`

`themes.yaml`의 전체 바스켓은 앱으로 내려오지 않아요. 대신 그달 날마다 `top` 5개에 **등장한 횟수**를 세서 많이 나온 순으로 보여줘요. 그래서 실제 바스켓과 다를 수 있다는 안내가 같이 나와요.

### 2-8. 색 5단계 — `bandVar()`

값의 절댓값이 클수록 진해져요. **5%를 상한**으로 두고, 그 넘는 값은 최강 단계로 고정해요.

```
a = min(|값|, 5) ÷ 5
a < 0.12         →  중립(회색)        |값| < 0.6%
a < 0.5          →  약한 강세/약세    0.6% ≤ |값| < 2.5%
그 외            →  강한 강세/약세    |값| ≥ 2.5%
```

| 구간 | 색 |
|---|---|
| −2.5% 이하 | 진한 파랑 |
| −2.5% ~ −0.6% | 연한 파랑 |
| −0.6% ~ +0.6% | 회색 |
| +0.6% ~ +2.5% | 연한 빨강 |
| +2.5% 이상 | 진한 빨강 |

한국 관행대로 **상승 빨강 / 하락 파랑**이에요. 값이 없으면(`null`) 회색으로 두고, 테마 전체가 비워진 날은 히트맵에 빗금으로 나와요.

---

## 3. 설정값 한눈에 (`themes.yaml`)

| 키 | 값 | 의미 |
|---|---|---|
| `aggregation.method` | `mean` | 테마 값은 단순평균 |
| `aggregation.outlier_clip` | `29.0` | 종목 등락률을 ±29%로 절사 |
| `aggregation.min_valid_ratio` | `0.6` | 유효 종목이 60% 미만이면 그날 그 테마는 비움 |
| `aggregation.top_n` | `5` | 테마별 상위 종목 저장 개수 |
| `aggregation.relative_to` | `kospi` | `rel`에서 뺄 기준 지수 |
| `indices` | kospi / kosdaq / nasdaq(QQQ) | 지수 3종 (나스닥은 직전 미국 세션) |

---

## 4. 알아두면 좋은 점

1. **월간 지수와 월간 테마 값은 종류가 달라요.** 지수는 복리(실제 월 수익률)고, 테마는 일별 값의 산술평균이에요. 그래서 "코스피 +3.39%, 화장품 +1.38%"를 그대로 견줘서 "화장품이 코스피보다 낮다"고 읽으면 안 돼요. (`rel` 기준이면 화장품의 +1.38은 "하루 평균 시장보다 1.38%p 더 올랐다"는 뜻이에요.)
2. **`lead`는 rel 고정, 월간 1위는 기준 연동.** 일별 `lead`는 배치에서 rel로 확정돼 저장되고, 월간 1위 테마와 저조 테마는 화면에서 그때그때 기준을 따라 다시 계산해요.
3. **저조 테마 표는 값이 없는 테마를 0%로 쳐요.** `computeWeakest()`는 유효한 날이 하나도 없는 테마의 평균을 0으로 두는데, 월간 1위(`summarizeMonth`)는 그런 테마를 건너뛰어요. 한 달 내내 비는 테마가 생기면 두 계산이 어긋날 수 있어요.
4. **유효 비율의 분모는 "수집에 성공한 종목 수"** 예요. `themes.yaml`에 적힌 전체 종목이 아니라, API에서 캔들을 받아온 종목만 세요. 종목코드가 틀려서 스킵된 종목이 있으면 바스켓이 그만큼 작아진 채로 계산돼요 (`index.json`의 `skippedTickers` 확인).
5. **`top`과 `chg`는 절사 후 값이에요.** ±29%를 넘긴 종목은 29로 표시돼요.
